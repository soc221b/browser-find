# Find

Monorepo for the browser find extension.

## Packages

- `@browser-find/core`: function-only DOM find and navigation helpers.
- `@browser-find/chrome-extension`: Chrome extension runtime/UI that consumes core.

## Commands

- `npm run build`: build extension package artifact.
- `npm run test`: run core unit tests, extension unit tests, then extension e2e tests.
