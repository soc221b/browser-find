{
  "name": "browser-find",
  "version": "0.9.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @browser-find/chrome-extension",
    "format": "npx prettier --write .",
    "format:check": "npx prettier --check .",
    "test": "npm run test:unit && npm run test:e2e",
    "test:e2e": "npm run test:e2e --workspace @browser-find/chrome-extension",
    "test:unit": "npm run test --workspace @browser-find/core && npm run test:unit --workspace @browser-find/chrome-extension"
  }
}
