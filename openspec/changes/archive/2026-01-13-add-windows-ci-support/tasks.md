## 1. CI Configuration

- [x] 1.1 Update `.github/workflows/ci.yml` to include a job or matrix strategy for running E2E tests on `windows-latest`.
- [x] 1.2 Configure the Windows job to run `npm run test:e2e` without `xvfb-run`.
